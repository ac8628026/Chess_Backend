{
    "version": "1.7",
    "builds": [
        {
            "src": "server.js",
            "use": "@now/static-build"
        }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "/server.js" }
    ]
}